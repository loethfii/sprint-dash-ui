version: '3.8'

services:
  sprint-dash-ui:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sprint-dash-ui
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - PORT=3001
      - HOST=0.0.0.0
